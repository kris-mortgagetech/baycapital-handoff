{
  "name": "elliemae-webhook-api",
  "version": "1.0.0",
  "description": "Azure SWA API — Ellie Mae webhook receiver + pub/sub queue processor",
  "scripts": {
    "start": "func start",
    "test": "jest"
  },
  "dependencies": {
    "@azure/storage-queue": "^12.18.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}